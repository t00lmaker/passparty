<!DOCTYPE html>
<html>
<head>
  <title>Pass Party</title>
  <!-- CSS only -->
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" />
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.3.0/font/bootstrap-icons.css" />
  <link rel="stylesheet" type="text/css" href="https://cdn.datatables.net/1.10.25/css/jquery.dataTables.css">
  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
  <style>
    .navbar-custom-shadow {
      box-shadow: 0 4px 8px 0 rgba(0,0,0,0.2), 0 6px 20px 0 rgba(0,0,0,0.19);
    }
    .container-custom-margin {
      margin-top: 3%; 
    }
    body {
      font-family: 'Roboto', sans-serif;
      color: #808080;
      margin: 0;
      padding: 0;
      background-color: #f0f0f0;
    }
    .container {
      width: 80%;
      margin: 3% auto;
      background-color: #fff;
      padding: 5%;
      border-radius: 8px;
      box-shadow: 0px 0px 10px 0px rgba(0,0,0,0.1);
    }
    h1, p {
      color: #333;
    }
    p {
      font-size: 1.2em;
    }
    .qr-code {
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100%;
    }
  </style>
    </head>
    <body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary shadow navbar-custom-shadow">
    <a class="navbar-brand" href="/guests">Pass Party</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav">
        <li class="nav-item active">
          <a class="nav-link" href="/guests">Lista de Convidados</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/guest">Castrar Covidado</a>
          </li>
          <li class="nav-item">
          <a class="nav-link" href="/guests/confirm">Validar Presença</a>
          </li>
          </ul>
          </div>
          </nav>
          <% if @message %>
          <div class="alert alert-<%= @message_type %>" role="alert">
          <%= @message %>  
          </div>
          <% end %>

          <div class="container container-custom-margin">
          <%= yield %>
          </div>
  <!-- JS, Popper.js, and jQuery -->

  <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js"></script>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"></script>

  </body>
  </html>